image: Visual Studio 2017

environment:
  nodejs_version: "Stable"

cache:
  - node_modules

install:
  - ps: Install-Product node $env:nodejs_version x64
  - npm install
  - cd svn
  - npm install
  - cd ..

build_script: npm run vscode:prepublish

test: off
